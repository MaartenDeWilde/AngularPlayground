define(['/AngularPlayground/js/controllers/booksController.js'], function(controller) {
    describe('just checking', function() {
        it('works for app', function() {           
            expect(1).toEqual(1);
        });
    });
});